name: CI — Pull Request

on:
  pull_request:
    branches:
      - dev
      - main

jobs:
  test:
    name: Tests
    uses: ./.github/workflows/run-tests.yml

  lint:
    name: Lint
    uses: ./.github/workflows/run-lint.yml
